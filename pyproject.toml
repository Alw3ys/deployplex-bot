[tool.poetry]
name = "helloworld"
version = "0.1.0"
description = "Hello World FastAPI example"
authors = ["DeployPlex Team <hi@deployplex.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.100.1"
uvicorn = {extras = ["standard"], version = "^0.23.2"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
